<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
    "-//Hibernate/Hibernate Configuration DTD//EN"
    "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
	<session-factory>

		<!-- JDBC related configuration -->
		<property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
		<property name="hibernate.connection.url">jdbc:mysql://localhost:3306/student_info</property>
		<property name="hibernate.connection.username">root</property>
		<property name="hibernate.connection.password">@kjl123OM#;010</property>
		
		<!-- 
		// Creates a table(if table is found then drop it and create new one)
		<property name="hibernate.hbm2ddl.auto">create</property>
		-->
		<!-- 
		// hibernate will drop and create a new table upon starting the application , once the application is stopped then the created table will be dropped
		<property name="hibernate.hbm2ddl.auto">create-drop</property>
		-->

		<!-- Creates a table(if table not found then create, otherwise reuse the existing one) -->
		<property name="hibernate.hbm2ddl.auto">create</property>

		<!-- Informing ORM to generate the query as per DBE -->
		<property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>
		<!-- To Display the query generated by ORM  -->
		<property name="hibernate.show_sql">true</property>
		<!-- To Display the query generated by ORM in proper format -->
		<property name="hibernate.format_sql">true</property>
		
		


	</session-factory>
</hibernate-configuration>